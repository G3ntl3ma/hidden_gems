# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Game(bases.BaseGame):
    """Represents a Game record"""

    id: _int
    name: _str
    required_age: _int
    is_free: _bool
    detailed_description: Optional[_str] = None
    about_the_game: Optional[_str] = None
    short_description: Optional[_str] = None
    supported_languages: Optional[_str] = None
    header_image: Optional[_str] = None
    developers: Optional[_str] = None
    publishers: Optional[_str] = None
    windows: _bool
    mac: _bool
    linux: _bool
    metacritic: Optional[_int] = None
    release_date: Optional[datetime.datetime] = None
    coming_soon: _bool
    owners_min: _int
    owners_max: _int
    average_2weeks: _int
    average_forever: _int
    reviewNumReviews: Optional[_int] = None
    reviewScore: Optional[_int] = None
    reviewScoreDesc: Optional[_str] = None
    reviewTotalPositive: Optional[_int] = None
    reviewTotalNegative: Optional[_int] = None
    reviewTotalReviews: Optional[_int] = None
    gameDevelopers: Optional[List['models.GameDeveloper']] = None
    gamePublishers: Optional[List['models.GamePublisher']] = None
    categories: Optional[List['models.GameCategory']] = None
    genres: Optional[List['models.GameGenre']] = None
    reviews: Optional[List['models.Review']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GameKeys']] = None,
        exclude: Optional[Iterable['types.GameKeys']] = None,
        required: Optional[Iterable['types.GameKeys']] = None,
        optional: Optional[Iterable['types.GameKeys']] = None,
        relations: Optional[Mapping['types.GameRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GameKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Game_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Game_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Game_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Game_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Game_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Game_relational_fields:
                        raise errors.UnknownRelationalFieldError('Game', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Game / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Game',
            }
        )
        _created_partial_types.add(name)


class Category(bases.BaseCategory):
    """Represents a Category record"""

    id: _int
    name: _str
    games: Optional[List['models.GameCategory']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CategoryKeys']] = None,
        exclude: Optional[Iterable['types.CategoryKeys']] = None,
        required: Optional[Iterable['types.CategoryKeys']] = None,
        optional: Optional[Iterable['types.CategoryKeys']] = None,
        relations: Optional[Mapping['types.CategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Category_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Category_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Category_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Category_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Category_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Category_relational_fields:
                        raise errors.UnknownRelationalFieldError('Category', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Category / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Category',
            }
        )
        _created_partial_types.add(name)


class GameCategory(bases.BaseGameCategory):
    """Represents a GameCategory record"""

    gameId: _int
    categoryId: _int
    game: Optional['models.Game'] = None
    category: Optional['models.Category'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GameCategoryKeys']] = None,
        exclude: Optional[Iterable['types.GameCategoryKeys']] = None,
        required: Optional[Iterable['types.GameCategoryKeys']] = None,
        optional: Optional[Iterable['types.GameCategoryKeys']] = None,
        relations: Optional[Mapping['types.GameCategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GameCategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GameCategory_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GameCategory_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GameCategory_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GameCategory_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GameCategory_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GameCategory_relational_fields:
                        raise errors.UnknownRelationalFieldError('GameCategory', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GameCategory / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GameCategory',
            }
        )
        _created_partial_types.add(name)


class Genre(bases.BaseGenre):
    """Represents a Genre record"""

    id: _int
    name: _str
    games: Optional[List['models.GameGenre']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GenreKeys']] = None,
        exclude: Optional[Iterable['types.GenreKeys']] = None,
        required: Optional[Iterable['types.GenreKeys']] = None,
        optional: Optional[Iterable['types.GenreKeys']] = None,
        relations: Optional[Mapping['types.GenreRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GenreKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Genre_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Genre_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Genre_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Genre_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Genre_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Genre_relational_fields:
                        raise errors.UnknownRelationalFieldError('Genre', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Genre / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Genre',
            }
        )
        _created_partial_types.add(name)


class GameGenre(bases.BaseGameGenre):
    """Represents a GameGenre record"""

    gameId: _int
    genreId: _int
    game: Optional['models.Game'] = None
    genre: Optional['models.Genre'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GameGenreKeys']] = None,
        exclude: Optional[Iterable['types.GameGenreKeys']] = None,
        required: Optional[Iterable['types.GameGenreKeys']] = None,
        optional: Optional[Iterable['types.GameGenreKeys']] = None,
        relations: Optional[Mapping['types.GameGenreRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GameGenreKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GameGenre_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GameGenre_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GameGenre_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GameGenre_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GameGenre_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GameGenre_relational_fields:
                        raise errors.UnknownRelationalFieldError('GameGenre', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GameGenre / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GameGenre',
            }
        )
        _created_partial_types.add(name)


class Developer(bases.BaseDeveloper):
    """Represents a Developer record"""

    name: _str
    amount_games_published: _int
    games: Optional[List['models.GameDeveloper']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DeveloperKeys']] = None,
        exclude: Optional[Iterable['types.DeveloperKeys']] = None,
        required: Optional[Iterable['types.DeveloperKeys']] = None,
        optional: Optional[Iterable['types.DeveloperKeys']] = None,
        relations: Optional[Mapping['types.DeveloperRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DeveloperKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Developer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Developer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Developer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Developer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Developer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Developer_relational_fields:
                        raise errors.UnknownRelationalFieldError('Developer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Developer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Developer',
            }
        )
        _created_partial_types.add(name)


class Publisher(bases.BasePublisher):
    """Represents a Publisher record"""

    name: _str
    amount_games_published: _int
    games: Optional[List['models.GamePublisher']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PublisherKeys']] = None,
        exclude: Optional[Iterable['types.PublisherKeys']] = None,
        required: Optional[Iterable['types.PublisherKeys']] = None,
        optional: Optional[Iterable['types.PublisherKeys']] = None,
        relations: Optional[Mapping['types.PublisherRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PublisherKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Publisher_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Publisher_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Publisher_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Publisher_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Publisher_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Publisher_relational_fields:
                        raise errors.UnknownRelationalFieldError('Publisher', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Publisher / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Publisher',
            }
        )
        _created_partial_types.add(name)


class GameDeveloper(bases.BaseGameDeveloper):
    """Represents a GameDeveloper record"""

    gameId: _int
    developerName: _str
    game: Optional['models.Game'] = None
    developer: Optional['models.Developer'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GameDeveloperKeys']] = None,
        exclude: Optional[Iterable['types.GameDeveloperKeys']] = None,
        required: Optional[Iterable['types.GameDeveloperKeys']] = None,
        optional: Optional[Iterable['types.GameDeveloperKeys']] = None,
        relations: Optional[Mapping['types.GameDeveloperRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GameDeveloperKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GameDeveloper_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GameDeveloper_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GameDeveloper_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GameDeveloper_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GameDeveloper_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GameDeveloper_relational_fields:
                        raise errors.UnknownRelationalFieldError('GameDeveloper', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GameDeveloper / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GameDeveloper',
            }
        )
        _created_partial_types.add(name)


class GamePublisher(bases.BaseGamePublisher):
    """Represents a GamePublisher record"""

    gameId: _int
    publisherName: _str
    game: Optional['models.Game'] = None
    publisher: Optional['models.Publisher'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.GamePublisherKeys']] = None,
        exclude: Optional[Iterable['types.GamePublisherKeys']] = None,
        required: Optional[Iterable['types.GamePublisherKeys']] = None,
        optional: Optional[Iterable['types.GamePublisherKeys']] = None,
        relations: Optional[Mapping['types.GamePublisherRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.GamePublisherKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _GamePublisher_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _GamePublisher_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _GamePublisher_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _GamePublisher_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _GamePublisher_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _GamePublisher_relational_fields:
                        raise errors.UnknownRelationalFieldError('GamePublisher', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid GamePublisher / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'GamePublisher',
            }
        )
        _created_partial_types.add(name)


class Review(bases.BaseReview):
    """Represents a Review record"""

    id: _str
    gameId: _int
    authorSteamId: Optional[_str] = None
    authorPlaytimeForever: _int
    authorPlaytimeAtReview: _int
    authorLastPlayed: Optional[datetime.datetime] = None
    language: Optional[_str] = None
    review: Optional[_str] = None
    votedUp: _bool
    votesUp: _int
    votesFunny: _int
    weightedVoteScore: Optional[_float] = None
    writtenDuringEarlyAccess: _bool
    game: Optional['models.Game'] = None
    timestampCreated: Optional[datetime.datetime] = None
    timestampUpdated: Optional[datetime.datetime] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ReviewKeys']] = None,
        exclude: Optional[Iterable['types.ReviewKeys']] = None,
        required: Optional[Iterable['types.ReviewKeys']] = None,
        optional: Optional[Iterable['types.ReviewKeys']] = None,
        relations: Optional[Mapping['types.ReviewRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ReviewKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Review_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Review_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Review_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Review_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Review_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Review_relational_fields:
                        raise errors.UnknownRelationalFieldError('Review', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Review / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Review',
            }
        )
        _created_partial_types.add(name)



_Game_relational_fields: Set[str] = {
        'gameDevelopers',
        'gamePublishers',
        'categories',
        'genres',
        'reviews',
    }
_Game_fields: Dict['types.GameKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('required_age', {
            'name': 'required_age',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('is_free', {
            'name': 'is_free',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('detailed_description', {
            'name': 'detailed_description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('about_the_game', {
            'name': 'about_the_game',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('short_description', {
            'name': 'short_description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('supported_languages', {
            'name': 'supported_languages',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('header_image', {
            'name': 'header_image',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('developers', {
            'name': 'developers',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('publishers', {
            'name': 'publishers',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('windows', {
            'name': 'windows',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('mac', {
            'name': 'mac',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('linux', {
            'name': 'linux',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('metacritic', {
            'name': 'metacritic',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('release_date', {
            'name': 'release_date',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('coming_soon', {
            'name': 'coming_soon',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('owners_min', {
            'name': 'owners_min',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('owners_max', {
            'name': 'owners_max',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('average_2weeks', {
            'name': 'average_2weeks',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('average_forever', {
            'name': 'average_forever',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('reviewNumReviews', {
            'name': 'reviewNumReviews',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('reviewScore', {
            'name': 'reviewScore',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('reviewScoreDesc', {
            'name': 'reviewScoreDesc',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('reviewTotalPositive', {
            'name': 'reviewTotalPositive',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('reviewTotalNegative', {
            'name': 'reviewTotalNegative',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('reviewTotalReviews', {
            'name': 'reviewTotalReviews',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('gameDevelopers', {
            'name': 'gameDevelopers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GameDeveloper\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('gamePublishers', {
            'name': 'gamePublishers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GamePublisher\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('categories', {
            'name': 'categories',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GameCategory\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('genres', {
            'name': 'genres',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GameGenre\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('reviews', {
            'name': 'reviews',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Review\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Category_relational_fields: Set[str] = {
        'games',
    }
_Category_fields: Dict['types.CategoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('games', {
            'name': 'games',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GameCategory\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GameCategory_relational_fields: Set[str] = {
        'game',
        'category',
    }
_GameCategory_fields: Dict['types.GameCategoryKeys', PartialModelField] = OrderedDict(
    [
        ('gameId', {
            'name': 'gameId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('game', {
            'name': 'game',
            'is_list': False,
            'optional': True,
            'type': 'models.Game',
            'is_relational': True,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.Category',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Genre_relational_fields: Set[str] = {
        'games',
    }
_Genre_fields: Dict['types.GenreKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('games', {
            'name': 'games',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GameGenre\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GameGenre_relational_fields: Set[str] = {
        'game',
        'genre',
    }
_GameGenre_fields: Dict['types.GameGenreKeys', PartialModelField] = OrderedDict(
    [
        ('gameId', {
            'name': 'gameId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('genreId', {
            'name': 'genreId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('game', {
            'name': 'game',
            'is_list': False,
            'optional': True,
            'type': 'models.Game',
            'is_relational': True,
            'documentation': None,
        }),
        ('genre', {
            'name': 'genre',
            'is_list': False,
            'optional': True,
            'type': 'models.Genre',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Developer_relational_fields: Set[str] = {
        'games',
    }
_Developer_fields: Dict['types.DeveloperKeys', PartialModelField] = OrderedDict(
    [
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount_games_published', {
            'name': 'amount_games_published',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('games', {
            'name': 'games',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GameDeveloper\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Publisher_relational_fields: Set[str] = {
        'games',
    }
_Publisher_fields: Dict['types.PublisherKeys', PartialModelField] = OrderedDict(
    [
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount_games_published', {
            'name': 'amount_games_published',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('games', {
            'name': 'games',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.GamePublisher\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GameDeveloper_relational_fields: Set[str] = {
        'game',
        'developer',
    }
_GameDeveloper_fields: Dict['types.GameDeveloperKeys', PartialModelField] = OrderedDict(
    [
        ('gameId', {
            'name': 'gameId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('developerName', {
            'name': 'developerName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('game', {
            'name': 'game',
            'is_list': False,
            'optional': True,
            'type': 'models.Game',
            'is_relational': True,
            'documentation': None,
        }),
        ('developer', {
            'name': 'developer',
            'is_list': False,
            'optional': True,
            'type': 'models.Developer',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_GamePublisher_relational_fields: Set[str] = {
        'game',
        'publisher',
    }
_GamePublisher_fields: Dict['types.GamePublisherKeys', PartialModelField] = OrderedDict(
    [
        ('gameId', {
            'name': 'gameId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('publisherName', {
            'name': 'publisherName',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('game', {
            'name': 'game',
            'is_list': False,
            'optional': True,
            'type': 'models.Game',
            'is_relational': True,
            'documentation': None,
        }),
        ('publisher', {
            'name': 'publisher',
            'is_list': False,
            'optional': True,
            'type': 'models.Publisher',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Review_relational_fields: Set[str] = {
        'game',
    }
_Review_fields: Dict['types.ReviewKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('gameId', {
            'name': 'gameId',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('authorSteamId', {
            'name': 'authorSteamId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('authorPlaytimeForever', {
            'name': 'authorPlaytimeForever',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('authorPlaytimeAtReview', {
            'name': 'authorPlaytimeAtReview',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('authorLastPlayed', {
            'name': 'authorLastPlayed',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('language', {
            'name': 'language',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('review', {
            'name': 'review',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('votedUp', {
            'name': 'votedUp',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('votesUp', {
            'name': 'votesUp',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('votesFunny', {
            'name': 'votesFunny',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('weightedVoteScore', {
            'name': 'weightedVoteScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('writtenDuringEarlyAccess', {
            'name': 'writtenDuringEarlyAccess',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('game', {
            'name': 'game',
            'is_list': False,
            'optional': True,
            'type': 'models.Game',
            'is_relational': True,
            'documentation': None,
        }),
        ('timestampCreated', {
            'name': 'timestampCreated',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('timestampUpdated', {
            'name': 'timestampUpdated',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Game)
model_rebuild(Category)
model_rebuild(GameCategory)
model_rebuild(Genre)
model_rebuild(GameGenre)
model_rebuild(Developer)
model_rebuild(Publisher)
model_rebuild(GameDeveloper)
model_rebuild(GamePublisher)
model_rebuild(Review)
