# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class GameActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Game]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Game.prisma().query_raw(
            'SELECT * FROM Game WHERE id = ?',
            1103527590,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Game
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Game.prisma().query_first(
            'SELECT * FROM Game WHERE name = ?',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.GameCreateInput,
        include: Optional[types.GameInclude] = None
    ) -> _PrismaModelT:
        """Create a new Game record.

        Parameters
        ----------
        data
            Game record data
        include
            Specifies which relations should be loaded on the returned Game model

        Returns
        -------
        prisma.models.Game
            The created Game record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Game record from just the required fields
        game = Game.prisma().create(
            data={
                # data to create a Game record
                'id': 662824084,
                'name': 'bbehjachib',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.GameCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Game records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Game record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Game.prisma().create_many(
            data=[
                {
                    # data to create a Game record
                    'id': 2035015474,
                    'name': 'dgiiaaijj',
                },
                {
                    # data to create a Game record
                    'id': 1508029952,
                    'name': 'eigcfgbif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.GameWhereUniqueInput,
        include: Optional[types.GameInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Game record.

        Parameters
        ----------
        where
            Game filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Game model

        Returns
        -------
        prisma.models.Game
            The deleted Game record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        game = Game.prisma().delete(
            where={
                'id': 1062517886,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.GameWhereUniqueInput,
        include: Optional[types.GameInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Game record.

        Parameters
        ----------
        where
            Game filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Game model

        Returns
        -------
        prisma.models.Game
            The found Game record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        game = Game.prisma().find_unique(
            where={
                'id': 267834847,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.GameWhereUniqueInput,
        include: Optional[types.GameInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Game record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Game filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Game model

        Returns
        -------
        prisma.models.Game
            The found Game record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        game = Game.prisma().find_unique_or_raise(
            where={
                'id': 180171308,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameWhereInput] = None,
        cursor: Optional[types.GameWhereUniqueInput] = None,
        include: Optional[types.GameInclude] = None,
        order: Optional[Union[types.GameOrderByInput, List[types.GameOrderByInput]]] = None,
        distinct: Optional[List[types.GameScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Game records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Game records returned
        skip
            Ignore the first N results
        where
            Game filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Game model
        order
            Order the returned Game records by any field
        distinct
            Filter Game records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Game]
            The list of all Game records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Game records
        games = Game.prisma().find_many(take=10)

        # find the first 5 Game records ordered by the required_age field
        games = Game.prisma().find_many(
            take=5,
            order={
                'required_age': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameWhereInput] = None,
        cursor: Optional[types.GameWhereUniqueInput] = None,
        include: Optional[types.GameInclude] = None,
        order: Optional[Union[types.GameOrderByInput, List[types.GameOrderByInput]]] = None,
        distinct: Optional[List[types.GameScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Game record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Game filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Game model
        order
            Order the returned Game records by any field
        distinct
            Filter Game records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Game
            The first Game record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Game record ordered by the is_free field
        game = Game.prisma().find_first(
            skip=1,
            order={
                'is_free': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameWhereInput] = None,
        cursor: Optional[types.GameWhereUniqueInput] = None,
        include: Optional[types.GameInclude] = None,
        order: Optional[Union[types.GameOrderByInput, List[types.GameOrderByInput]]] = None,
        distinct: Optional[List[types.GameScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Game record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Game filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Game model
        order
            Order the returned Game records by any field
        distinct
            Filter Game records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Game
            The first Game record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Game record ordered by the detailed_description field
        game = Game.prisma().find_first_or_raise(
            skip=1,
            order={
                'detailed_description': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.GameUpdateInput,
        where: types.GameWhereUniqueInput,
        include: Optional[types.GameInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Game record.

        Parameters
        ----------
        data
            Game record data specifying what to update
        where
            Game filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Game model

        Returns
        -------
        prisma.models.Game
            The updated Game record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        game = Game.prisma().update(
            where={
                'id': 836760821,
            },
            data={
                # data to update the Game record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.GameWhereUniqueInput,
        data: types.GameUpsertInput,
        include: Optional[types.GameInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Game filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Game model

        Returns
        -------
        prisma.models.Game
            The created or updated Game record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        game = Game.prisma().upsert(
            where={
                'id': 595337866,
            },
            data={
                'create': {
                    'id': 595337866,
                    'name': 'eigcfgbif',
                },
                'update': {
                    'name': 'eigcfgbif',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.GameUpdateManyMutationInput,
        where: types.GameWhereInput,
    ) -> int:
        """Update multiple Game records

        Parameters
        ----------
        data
            Game data to update the selected Game records to
        where
            Filter to select the Game records to update

        Returns
        -------
        int
            The total number of Game records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Game records
        total = Game.prisma().update_many(
            data={
                'about_the_game': 'hjaecfifb'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameWhereInput] = None,
        cursor: Optional[types.GameWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Game records present in the database

        Parameters
        ----------
        select
            Select the Game fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Game filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Game.prisma().count()

        # results: prisma.types.GameCountAggregateOutput
        results = Game.prisma().count(
            select={
                '_all': True,
                'short_description': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.GameCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameWhereInput] = None,
        cursor: Optional[types.GameWhereUniqueInput] = None,
    ) -> types.GameCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.GameCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameWhereInput] = None,
        cursor: Optional[types.GameWhereUniqueInput] = None,
    ) -> Union[int, types.GameCountAggregateOutput]:
        """Count the number of Game records present in the database

        Parameters
        ----------
        select
            Select the Game fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Game filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Game.prisma().count()

        # results: prisma.types.GameCountAggregateOutput
        results = Game.prisma().count(
            select={
                '_all': True,
                'supported_languages': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.GameCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.GameWhereInput] = None
    ) -> int:
        """Delete multiple Game records.

        Parameters
        ----------
        where
            Optional Game filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Game records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Game records
        total = Game.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.GameScalarFieldKeys'],
        *,
        where: Optional['types.GameWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.GameAvgAggregateInput'] = None,
        sum: Optional['types.GameSumAggregateInput'] = None,
        min: Optional['types.GameMinAggregateInput'] = None,
        max: Optional['types.GameMaxAggregateInput'] = None,
        having: Optional['types.GameScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.GameCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.GameScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.GameScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.GameGroupByOutput']:
        """Group Game records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Game fields to group records by
        where
            Game filter to select records
        take
            Limit the maximum number of Game records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.GameGroupByOutput]
            A list of dictionaries representing the Game record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Game records by header_image values
        # and count how many records are in each group
        results = Game.prisma().group_by(
            ['header_image'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class CategoryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Category]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Category.prisma().query_raw(
            'SELECT * FROM Category WHERE id = ?',
            2111915288,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Category
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Category.prisma().query_first(
            'SELECT * FROM Category WHERE name = ?',
            'bbejhfidcb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.CategoryCreateInput,
        include: Optional[types.CategoryInclude] = None
    ) -> _PrismaModelT:
        """Create a new Category record.

        Parameters
        ----------
        data
            Category record data
        include
            Specifies which relations should be loaded on the returned Category model

        Returns
        -------
        prisma.models.Category
            The created Category record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Category record from just the required fields
        category = Category.prisma().create(
            data={
                # data to create a Category record
                'id': 1644289366,
                'name': 'bdiicjafbj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.CategoryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Category records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Category record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Category.prisma().create_many(
            data=[
                {
                    # data to create a Category record
                    'id': 1647418052,
                    'name': 'bghffegacj',
                },
                {
                    # data to create a Category record
                    'id': 1767274722,
                    'name': 'dcgchcbbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.CategoryWhereUniqueInput,
        include: Optional[types.CategoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Category record.

        Parameters
        ----------
        where
            Category filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Category model

        Returns
        -------
        prisma.models.Category
            The deleted Category record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        category = Category.prisma().delete(
            where={
                'id': 1343201072,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.CategoryWhereUniqueInput,
        include: Optional[types.CategoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Category record.

        Parameters
        ----------
        where
            Category filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Category model

        Returns
        -------
        prisma.models.Category
            The found Category record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        category = Category.prisma().find_unique(
            where={
                'id': 675780521,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.CategoryWhereUniqueInput,
        include: Optional[types.CategoryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Category record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Category filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Category model

        Returns
        -------
        prisma.models.Category
            The found Category record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        category = Category.prisma().find_unique_or_raise(
            where={
                'id': 744964398,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CategoryWhereInput] = None,
        cursor: Optional[types.CategoryWhereUniqueInput] = None,
        include: Optional[types.CategoryInclude] = None,
        order: Optional[Union[types.CategoryOrderByInput, List[types.CategoryOrderByInput]]] = None,
        distinct: Optional[List[types.CategoryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Category records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Category records returned
        skip
            Ignore the first N results
        where
            Category filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Category model
        order
            Order the returned Category records by any field
        distinct
            Filter Category records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Category]
            The list of all Category records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Category records
        categorys = Category.prisma().find_many(take=10)

        # find the first 5 Category records ordered by the id field
        categorys = Category.prisma().find_many(
            take=5,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CategoryWhereInput] = None,
        cursor: Optional[types.CategoryWhereUniqueInput] = None,
        include: Optional[types.CategoryInclude] = None,
        order: Optional[Union[types.CategoryOrderByInput, List[types.CategoryOrderByInput]]] = None,
        distinct: Optional[List[types.CategoryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Category record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Category filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Category model
        order
            Order the returned Category records by any field
        distinct
            Filter Category records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Category
            The first Category record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Category record ordered by the name field
        category = Category.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CategoryWhereInput] = None,
        cursor: Optional[types.CategoryWhereUniqueInput] = None,
        include: Optional[types.CategoryInclude] = None,
        order: Optional[Union[types.CategoryOrderByInput, List[types.CategoryOrderByInput]]] = None,
        distinct: Optional[List[types.CategoryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Category record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Category filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Category model
        order
            Order the returned Category records by any field
        distinct
            Filter Category records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Category
            The first Category record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Category record ordered by the id field
        category = Category.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.CategoryUpdateInput,
        where: types.CategoryWhereUniqueInput,
        include: Optional[types.CategoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Category record.

        Parameters
        ----------
        data
            Category record data specifying what to update
        where
            Category filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Category model

        Returns
        -------
        prisma.models.Category
            The updated Category record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        category = Category.prisma().update(
            where={
                'id': 1969681615,
            },
            data={
                # data to update the Category record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.CategoryWhereUniqueInput,
        data: types.CategoryUpsertInput,
        include: Optional[types.CategoryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Category filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Category model

        Returns
        -------
        prisma.models.Category
            The created or updated Category record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        category = Category.prisma().upsert(
            where={
                'id': 1116175964,
            },
            data={
                'create': {
                    'id': 1116175964,
                    'name': 'dcgchcbbf',
                },
                'update': {
                    'name': 'dcgchcbbf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.CategoryUpdateManyMutationInput,
        where: types.CategoryWhereInput,
    ) -> int:
        """Update multiple Category records

        Parameters
        ----------
        data
            Category data to update the selected Category records to
        where
            Filter to select the Category records to update

        Returns
        -------
        int
            The total number of Category records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Category records
        total = Category.prisma().update_many(
            data={
                'name': 'igbehcbab'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CategoryWhereInput] = None,
        cursor: Optional[types.CategoryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Category records present in the database

        Parameters
        ----------
        select
            Select the Category fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Category filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CategoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Category.prisma().count()

        # results: prisma.types.CategoryCountAggregateOutput
        results = Category.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.CategoryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CategoryWhereInput] = None,
        cursor: Optional[types.CategoryWhereUniqueInput] = None,
    ) -> types.CategoryCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.CategoryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CategoryWhereInput] = None,
        cursor: Optional[types.CategoryWhereUniqueInput] = None,
    ) -> Union[int, types.CategoryCountAggregateOutput]:
        """Count the number of Category records present in the database

        Parameters
        ----------
        select
            Select the Category fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Category filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CategoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Category.prisma().count()

        # results: prisma.types.CategoryCountAggregateOutput
        results = Category.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.CategoryCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.CategoryWhereInput] = None
    ) -> int:
        """Delete multiple Category records.

        Parameters
        ----------
        where
            Optional Category filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Category records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Category records
        total = Category.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.CategoryScalarFieldKeys'],
        *,
        where: Optional['types.CategoryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.CategoryAvgAggregateInput'] = None,
        sum: Optional['types.CategorySumAggregateInput'] = None,
        min: Optional['types.CategoryMinAggregateInput'] = None,
        max: Optional['types.CategoryMaxAggregateInput'] = None,
        having: Optional['types.CategoryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.CategoryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.CategoryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.CategoryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.CategoryGroupByOutput']:
        """Group Category records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Category fields to group records by
        where
            Category filter to select records
        take
            Limit the maximum number of Category records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.CategoryGroupByOutput]
            A list of dictionaries representing the Category record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Category records by id values
        # and count how many records are in each group
        results = Category.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class GameCategoryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.GameCategory]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = GameCategory.prisma().query_raw(
            'SELECT * FROM GameCategory WHERE gameId = ?',
            1303003706,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.GameCategory
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = GameCategory.prisma().query_first(
            'SELECT * FROM GameCategory WHERE categoryId = ?',
            1686638315,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.GameCategoryCreateInput,
        include: Optional[types.GameCategoryInclude] = None
    ) -> _PrismaModelT:
        """Create a new GameCategory record.

        Parameters
        ----------
        data
            GameCategory record data
        include
            Specifies which relations should be loaded on the returned GameCategory model

        Returns
        -------
        prisma.models.GameCategory
            The created GameCategory record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a GameCategory record from just the required fields
        gamecategory = GameCategory.prisma().create(
            data={
                # data to create a GameCategory record
                'gameId': 2000430152,
                'categoryId': 1868141281,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.GameCategoryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple GameCategory records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of GameCategory record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = GameCategory.prisma().create_many(
            data=[
                {
                    # data to create a GameCategory record
                    'gameId': 1860847622,
                    'categoryId': 1448521415,
                },
                {
                    # data to create a GameCategory record
                    'gameId': 1628650740,
                    'categoryId': 1249606685,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.GameCategoryWhereUniqueInput,
        include: Optional[types.GameCategoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single GameCategory record.

        Parameters
        ----------
        where
            GameCategory filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned GameCategory model

        Returns
        -------
        prisma.models.GameCategory
            The deleted GameCategory record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamecategory = GameCategory.prisma().delete(
            where={
                # GameCategory where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.GameCategoryWhereUniqueInput,
        include: Optional[types.GameCategoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique GameCategory record.

        Parameters
        ----------
        where
            GameCategory filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameCategory model

        Returns
        -------
        prisma.models.GameCategory
            The found GameCategory record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamecategory = GameCategory.prisma().find_unique(
            where={
                # GameCategory where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.GameCategoryWhereUniqueInput,
        include: Optional[types.GameCategoryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique GameCategory record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            GameCategory filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameCategory model

        Returns
        -------
        prisma.models.GameCategory
            The found GameCategory record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamecategory = GameCategory.prisma().find_unique_or_raise(
            where={
                # GameCategory where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameCategoryWhereInput] = None,
        cursor: Optional[types.GameCategoryWhereUniqueInput] = None,
        include: Optional[types.GameCategoryInclude] = None,
        order: Optional[Union[types.GameCategoryOrderByInput, List[types.GameCategoryOrderByInput]]] = None,
        distinct: Optional[List[types.GameCategoryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple GameCategory records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of GameCategory records returned
        skip
            Ignore the first N results
        where
            GameCategory filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameCategory model
        order
            Order the returned GameCategory records by any field
        distinct
            Filter GameCategory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.GameCategory]
            The list of all GameCategory records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 GameCategory records
        gamecategorys = GameCategory.prisma().find_many(take=10)

        # find the first 5 GameCategory records ordered by the gameId field
        gamecategorys = GameCategory.prisma().find_many(
            take=5,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameCategoryWhereInput] = None,
        cursor: Optional[types.GameCategoryWhereUniqueInput] = None,
        include: Optional[types.GameCategoryInclude] = None,
        order: Optional[Union[types.GameCategoryOrderByInput, List[types.GameCategoryOrderByInput]]] = None,
        distinct: Optional[List[types.GameCategoryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single GameCategory record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameCategory filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameCategory model
        order
            Order the returned GameCategory records by any field
        distinct
            Filter GameCategory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameCategory
            The first GameCategory record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameCategory record ordered by the categoryId field
        gamecategory = GameCategory.prisma().find_first(
            skip=1,
            order={
                'categoryId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameCategoryWhereInput] = None,
        cursor: Optional[types.GameCategoryWhereUniqueInput] = None,
        include: Optional[types.GameCategoryInclude] = None,
        order: Optional[Union[types.GameCategoryOrderByInput, List[types.GameCategoryOrderByInput]]] = None,
        distinct: Optional[List[types.GameCategoryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single GameCategory record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameCategory filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameCategory model
        order
            Order the returned GameCategory records by any field
        distinct
            Filter GameCategory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameCategory
            The first GameCategory record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameCategory record ordered by the gameId field
        gamecategory = GameCategory.prisma().find_first_or_raise(
            skip=1,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.GameCategoryUpdateInput,
        where: types.GameCategoryWhereUniqueInput,
        include: Optional[types.GameCategoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single GameCategory record.

        Parameters
        ----------
        data
            GameCategory record data specifying what to update
        where
            GameCategory filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned GameCategory model

        Returns
        -------
        prisma.models.GameCategory
            The updated GameCategory record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        gamecategory = GameCategory.prisma().update(
            where={
                # GameCategory where unique filter

            },
            data={
                # data to update the GameCategory record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.GameCategoryWhereUniqueInput,
        data: types.GameCategoryUpsertInput,
        include: Optional[types.GameCategoryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            GameCategory filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned GameCategory model

        Returns
        -------
        prisma.models.GameCategory
            The created or updated GameCategory record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamecategory = GameCategory.prisma().upsert(
            where={
                # GameCategory where unique filter
            },
            data={
                'create': {
                    # GameCategory data to be set if the record does not exist
                },
                'update': {
                    # GameCategory data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.GameCategoryUpdateManyMutationInput,
        where: types.GameCategoryWhereInput,
    ) -> int:
        """Update multiple GameCategory records

        Parameters
        ----------
        data
            GameCategory data to update the selected GameCategory records to
        where
            Filter to select the GameCategory records to update

        Returns
        -------
        int
            The total number of GameCategory records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all GameCategory records
        total = GameCategory.prisma().update_many(
            data={
                'categoryId': 835903122
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameCategoryWhereInput] = None,
        cursor: Optional[types.GameCategoryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of GameCategory records present in the database

        Parameters
        ----------
        select
            Select the GameCategory fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameCategory filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameCategoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GameCategory.prisma().count()

        # results: prisma.types.GameCategoryCountAggregateOutput
        results = GameCategory.prisma().count(
            select={
                '_all': True,
                'gameId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.GameCategoryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameCategoryWhereInput] = None,
        cursor: Optional[types.GameCategoryWhereUniqueInput] = None,
    ) -> types.GameCategoryCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.GameCategoryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameCategoryWhereInput] = None,
        cursor: Optional[types.GameCategoryWhereUniqueInput] = None,
    ) -> Union[int, types.GameCategoryCountAggregateOutput]:
        """Count the number of GameCategory records present in the database

        Parameters
        ----------
        select
            Select the GameCategory fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameCategory filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameCategoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GameCategory.prisma().count()

        # results: prisma.types.GameCategoryCountAggregateOutput
        results = GameCategory.prisma().count(
            select={
                '_all': True,
                'categoryId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.GameCategoryCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.GameCategoryWhereInput] = None
    ) -> int:
        """Delete multiple GameCategory records.

        Parameters
        ----------
        where
            Optional GameCategory filter to find the records to be deleted

        Returns
        -------
        int
            The total number of GameCategory records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all GameCategory records
        total = GameCategory.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.GameCategoryScalarFieldKeys'],
        *,
        where: Optional['types.GameCategoryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.GameCategoryAvgAggregateInput'] = None,
        sum: Optional['types.GameCategorySumAggregateInput'] = None,
        min: Optional['types.GameCategoryMinAggregateInput'] = None,
        max: Optional['types.GameCategoryMaxAggregateInput'] = None,
        having: Optional['types.GameCategoryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.GameCategoryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.GameCategoryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.GameCategoryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.GameCategoryGroupByOutput']:
        """Group GameCategory records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar GameCategory fields to group records by
        where
            GameCategory filter to select records
        take
            Limit the maximum number of GameCategory records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.GameCategoryGroupByOutput]
            A list of dictionaries representing the GameCategory record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group GameCategory records by gameId values
        # and count how many records are in each group
        results = GameCategory.prisma().group_by(
            ['gameId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class GenreActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Genre]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Genre.prisma().query_raw(
            'SELECT * FROM Genre WHERE id = ?',
            763719779,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Genre
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Genre.prisma().query_first(
            'SELECT * FROM Genre WHERE name = ?',
            'ecjjjfbae',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.GenreCreateInput,
        include: Optional[types.GenreInclude] = None
    ) -> _PrismaModelT:
        """Create a new Genre record.

        Parameters
        ----------
        data
            Genre record data
        include
            Specifies which relations should be loaded on the returned Genre model

        Returns
        -------
        prisma.models.Genre
            The created Genre record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Genre record from just the required fields
        genre = Genre.prisma().create(
            data={
                # data to create a Genre record
                'id': 1775811865,
                'name': 'ijdbeffgg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.GenreCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Genre records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Genre record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Genre.prisma().create_many(
            data=[
                {
                    # data to create a Genre record
                    'id': 995405759,
                    'name': 'cbachdgfce',
                },
                {
                    # data to create a Genre record
                    'id': 271520213,
                    'name': 'efggddide',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.GenreWhereUniqueInput,
        include: Optional[types.GenreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Genre record.

        Parameters
        ----------
        where
            Genre filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Genre model

        Returns
        -------
        prisma.models.Genre
            The deleted Genre record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        genre = Genre.prisma().delete(
            where={
                'id': 2058258651,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.GenreWhereUniqueInput,
        include: Optional[types.GenreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Genre record.

        Parameters
        ----------
        where
            Genre filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Genre model

        Returns
        -------
        prisma.models.Genre
            The found Genre record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        genre = Genre.prisma().find_unique(
            where={
                'id': 1583689592,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.GenreWhereUniqueInput,
        include: Optional[types.GenreInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Genre record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Genre filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Genre model

        Returns
        -------
        prisma.models.Genre
            The found Genre record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        genre = Genre.prisma().find_unique_or_raise(
            where={
                'id': 878442065,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GenreWhereInput] = None,
        cursor: Optional[types.GenreWhereUniqueInput] = None,
        include: Optional[types.GenreInclude] = None,
        order: Optional[Union[types.GenreOrderByInput, List[types.GenreOrderByInput]]] = None,
        distinct: Optional[List[types.GenreScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Genre records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Genre records returned
        skip
            Ignore the first N results
        where
            Genre filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Genre model
        order
            Order the returned Genre records by any field
        distinct
            Filter Genre records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Genre]
            The list of all Genre records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Genre records
        genres = Genre.prisma().find_many(take=10)

        # find the first 5 Genre records ordered by the id field
        genres = Genre.prisma().find_many(
            take=5,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GenreWhereInput] = None,
        cursor: Optional[types.GenreWhereUniqueInput] = None,
        include: Optional[types.GenreInclude] = None,
        order: Optional[Union[types.GenreOrderByInput, List[types.GenreOrderByInput]]] = None,
        distinct: Optional[List[types.GenreScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Genre record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Genre filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Genre model
        order
            Order the returned Genre records by any field
        distinct
            Filter Genre records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Genre
            The first Genre record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Genre record ordered by the name field
        genre = Genre.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GenreWhereInput] = None,
        cursor: Optional[types.GenreWhereUniqueInput] = None,
        include: Optional[types.GenreInclude] = None,
        order: Optional[Union[types.GenreOrderByInput, List[types.GenreOrderByInput]]] = None,
        distinct: Optional[List[types.GenreScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Genre record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Genre filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Genre model
        order
            Order the returned Genre records by any field
        distinct
            Filter Genre records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Genre
            The first Genre record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Genre record ordered by the id field
        genre = Genre.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.GenreUpdateInput,
        where: types.GenreWhereUniqueInput,
        include: Optional[types.GenreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Genre record.

        Parameters
        ----------
        data
            Genre record data specifying what to update
        where
            Genre filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Genre model

        Returns
        -------
        prisma.models.Genre
            The updated Genre record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        genre = Genre.prisma().update(
            where={
                'id': 1675280054,
            },
            data={
                # data to update the Genre record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.GenreWhereUniqueInput,
        data: types.GenreUpsertInput,
        include: Optional[types.GenreInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Genre filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Genre model

        Returns
        -------
        prisma.models.Genre
            The created or updated Genre record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        genre = Genre.prisma().upsert(
            where={
                'id': 1627576247,
            },
            data={
                'create': {
                    'id': 1627576247,
                    'name': 'efggddide',
                },
                'update': {
                    'name': 'efggddide',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.GenreUpdateManyMutationInput,
        where: types.GenreWhereInput,
    ) -> int:
        """Update multiple Genre records

        Parameters
        ----------
        data
            Genre data to update the selected Genre records to
        where
            Filter to select the Genre records to update

        Returns
        -------
        int
            The total number of Genre records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Genre records
        total = Genre.prisma().update_many(
            data={
                'name': 'cafeiaccbc'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GenreWhereInput] = None,
        cursor: Optional[types.GenreWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Genre records present in the database

        Parameters
        ----------
        select
            Select the Genre fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Genre filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GenreCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Genre.prisma().count()

        # results: prisma.types.GenreCountAggregateOutput
        results = Genre.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.GenreCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GenreWhereInput] = None,
        cursor: Optional[types.GenreWhereUniqueInput] = None,
    ) -> types.GenreCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.GenreCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GenreWhereInput] = None,
        cursor: Optional[types.GenreWhereUniqueInput] = None,
    ) -> Union[int, types.GenreCountAggregateOutput]:
        """Count the number of Genre records present in the database

        Parameters
        ----------
        select
            Select the Genre fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Genre filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GenreCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Genre.prisma().count()

        # results: prisma.types.GenreCountAggregateOutput
        results = Genre.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.GenreCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.GenreWhereInput] = None
    ) -> int:
        """Delete multiple Genre records.

        Parameters
        ----------
        where
            Optional Genre filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Genre records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Genre records
        total = Genre.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.GenreScalarFieldKeys'],
        *,
        where: Optional['types.GenreWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.GenreAvgAggregateInput'] = None,
        sum: Optional['types.GenreSumAggregateInput'] = None,
        min: Optional['types.GenreMinAggregateInput'] = None,
        max: Optional['types.GenreMaxAggregateInput'] = None,
        having: Optional['types.GenreScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.GenreCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.GenreScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.GenreScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.GenreGroupByOutput']:
        """Group Genre records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Genre fields to group records by
        where
            Genre filter to select records
        take
            Limit the maximum number of Genre records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.GenreGroupByOutput]
            A list of dictionaries representing the Genre record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Genre records by id values
        # and count how many records are in each group
        results = Genre.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class GameGenreActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.GameGenre]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = GameGenre.prisma().query_raw(
            'SELECT * FROM GameGenre WHERE gameId = ?',
            60335757,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.GameGenre
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = GameGenre.prisma().query_first(
            'SELECT * FROM GameGenre WHERE genreId = ?',
            684462146,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.GameGenreCreateInput,
        include: Optional[types.GameGenreInclude] = None
    ) -> _PrismaModelT:
        """Create a new GameGenre record.

        Parameters
        ----------
        data
            GameGenre record data
        include
            Specifies which relations should be loaded on the returned GameGenre model

        Returns
        -------
        prisma.models.GameGenre
            The created GameGenre record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a GameGenre record from just the required fields
        gamegenre = GameGenre.prisma().create(
            data={
                # data to create a GameGenre record
                'gameId': 1625503827,
                'genreId': 521827728,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.GameGenreCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple GameGenre records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of GameGenre record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = GameGenre.prisma().create_many(
            data=[
                {
                    # data to create a GameGenre record
                    'gameId': 1266032265,
                    'genreId': 93253262,
                },
                {
                    # data to create a GameGenre record
                    'gameId': 2053047983,
                    'genreId': 685333180,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.GameGenreWhereUniqueInput,
        include: Optional[types.GameGenreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single GameGenre record.

        Parameters
        ----------
        where
            GameGenre filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned GameGenre model

        Returns
        -------
        prisma.models.GameGenre
            The deleted GameGenre record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamegenre = GameGenre.prisma().delete(
            where={
                # GameGenre where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.GameGenreWhereUniqueInput,
        include: Optional[types.GameGenreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique GameGenre record.

        Parameters
        ----------
        where
            GameGenre filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameGenre model

        Returns
        -------
        prisma.models.GameGenre
            The found GameGenre record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamegenre = GameGenre.prisma().find_unique(
            where={
                # GameGenre where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.GameGenreWhereUniqueInput,
        include: Optional[types.GameGenreInclude] = None
    ) -> _PrismaModelT:
        """Find a unique GameGenre record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            GameGenre filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameGenre model

        Returns
        -------
        prisma.models.GameGenre
            The found GameGenre record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamegenre = GameGenre.prisma().find_unique_or_raise(
            where={
                # GameGenre where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameGenreWhereInput] = None,
        cursor: Optional[types.GameGenreWhereUniqueInput] = None,
        include: Optional[types.GameGenreInclude] = None,
        order: Optional[Union[types.GameGenreOrderByInput, List[types.GameGenreOrderByInput]]] = None,
        distinct: Optional[List[types.GameGenreScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple GameGenre records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of GameGenre records returned
        skip
            Ignore the first N results
        where
            GameGenre filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameGenre model
        order
            Order the returned GameGenre records by any field
        distinct
            Filter GameGenre records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.GameGenre]
            The list of all GameGenre records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 GameGenre records
        gamegenres = GameGenre.prisma().find_many(take=10)

        # find the first 5 GameGenre records ordered by the gameId field
        gamegenres = GameGenre.prisma().find_many(
            take=5,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameGenreWhereInput] = None,
        cursor: Optional[types.GameGenreWhereUniqueInput] = None,
        include: Optional[types.GameGenreInclude] = None,
        order: Optional[Union[types.GameGenreOrderByInput, List[types.GameGenreOrderByInput]]] = None,
        distinct: Optional[List[types.GameGenreScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single GameGenre record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameGenre filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameGenre model
        order
            Order the returned GameGenre records by any field
        distinct
            Filter GameGenre records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameGenre
            The first GameGenre record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameGenre record ordered by the genreId field
        gamegenre = GameGenre.prisma().find_first(
            skip=1,
            order={
                'genreId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameGenreWhereInput] = None,
        cursor: Optional[types.GameGenreWhereUniqueInput] = None,
        include: Optional[types.GameGenreInclude] = None,
        order: Optional[Union[types.GameGenreOrderByInput, List[types.GameGenreOrderByInput]]] = None,
        distinct: Optional[List[types.GameGenreScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single GameGenre record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameGenre filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameGenre model
        order
            Order the returned GameGenre records by any field
        distinct
            Filter GameGenre records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameGenre
            The first GameGenre record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameGenre record ordered by the gameId field
        gamegenre = GameGenre.prisma().find_first_or_raise(
            skip=1,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.GameGenreUpdateInput,
        where: types.GameGenreWhereUniqueInput,
        include: Optional[types.GameGenreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single GameGenre record.

        Parameters
        ----------
        data
            GameGenre record data specifying what to update
        where
            GameGenre filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned GameGenre model

        Returns
        -------
        prisma.models.GameGenre
            The updated GameGenre record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        gamegenre = GameGenre.prisma().update(
            where={
                # GameGenre where unique filter

            },
            data={
                # data to update the GameGenre record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.GameGenreWhereUniqueInput,
        data: types.GameGenreUpsertInput,
        include: Optional[types.GameGenreInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            GameGenre filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned GameGenre model

        Returns
        -------
        prisma.models.GameGenre
            The created or updated GameGenre record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamegenre = GameGenre.prisma().upsert(
            where={
                # GameGenre where unique filter
            },
            data={
                'create': {
                    # GameGenre data to be set if the record does not exist
                },
                'update': {
                    # GameGenre data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.GameGenreUpdateManyMutationInput,
        where: types.GameGenreWhereInput,
    ) -> int:
        """Update multiple GameGenre records

        Parameters
        ----------
        data
            GameGenre data to update the selected GameGenre records to
        where
            Filter to select the GameGenre records to update

        Returns
        -------
        int
            The total number of GameGenre records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all GameGenre records
        total = GameGenre.prisma().update_many(
            data={
                'genreId': 127474245
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameGenreWhereInput] = None,
        cursor: Optional[types.GameGenreWhereUniqueInput] = None,
    ) -> int:
        """Count the number of GameGenre records present in the database

        Parameters
        ----------
        select
            Select the GameGenre fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameGenre filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameGenreCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GameGenre.prisma().count()

        # results: prisma.types.GameGenreCountAggregateOutput
        results = GameGenre.prisma().count(
            select={
                '_all': True,
                'gameId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.GameGenreCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameGenreWhereInput] = None,
        cursor: Optional[types.GameGenreWhereUniqueInput] = None,
    ) -> types.GameGenreCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.GameGenreCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameGenreWhereInput] = None,
        cursor: Optional[types.GameGenreWhereUniqueInput] = None,
    ) -> Union[int, types.GameGenreCountAggregateOutput]:
        """Count the number of GameGenre records present in the database

        Parameters
        ----------
        select
            Select the GameGenre fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameGenre filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameGenreCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GameGenre.prisma().count()

        # results: prisma.types.GameGenreCountAggregateOutput
        results = GameGenre.prisma().count(
            select={
                '_all': True,
                'genreId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.GameGenreCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.GameGenreWhereInput] = None
    ) -> int:
        """Delete multiple GameGenre records.

        Parameters
        ----------
        where
            Optional GameGenre filter to find the records to be deleted

        Returns
        -------
        int
            The total number of GameGenre records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all GameGenre records
        total = GameGenre.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.GameGenreScalarFieldKeys'],
        *,
        where: Optional['types.GameGenreWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.GameGenreAvgAggregateInput'] = None,
        sum: Optional['types.GameGenreSumAggregateInput'] = None,
        min: Optional['types.GameGenreMinAggregateInput'] = None,
        max: Optional['types.GameGenreMaxAggregateInput'] = None,
        having: Optional['types.GameGenreScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.GameGenreCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.GameGenreScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.GameGenreScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.GameGenreGroupByOutput']:
        """Group GameGenre records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar GameGenre fields to group records by
        where
            GameGenre filter to select records
        take
            Limit the maximum number of GameGenre records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.GameGenreGroupByOutput]
            A list of dictionaries representing the GameGenre record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group GameGenre records by gameId values
        # and count how many records are in each group
        results = GameGenre.prisma().group_by(
            ['gameId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DeveloperActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Developer]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Developer.prisma().query_raw(
            'SELECT * FROM Developer WHERE name = ?',
            'jeijcbhfe',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Developer
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Developer.prisma().query_first(
            'SELECT * FROM Developer WHERE amount_games_published = ?',
            1964990155,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.DeveloperCreateInput,
        include: Optional[types.DeveloperInclude] = None
    ) -> _PrismaModelT:
        """Create a new Developer record.

        Parameters
        ----------
        data
            Developer record data
        include
            Specifies which relations should be loaded on the returned Developer model

        Returns
        -------
        prisma.models.Developer
            The created Developer record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Developer record from just the required fields
        developer = Developer.prisma().create(
            data={
                # data to create a Developer record
                'name': 'bcciijbibg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.DeveloperCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Developer records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Developer record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Developer.prisma().create_many(
            data=[
                {
                    # data to create a Developer record
                    'name': 'cffcachfd',
                },
                {
                    # data to create a Developer record
                    'name': 'bccdfhdigc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.DeveloperWhereUniqueInput,
        include: Optional[types.DeveloperInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Developer record.

        Parameters
        ----------
        where
            Developer filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Developer model

        Returns
        -------
        prisma.models.Developer
            The deleted Developer record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        developer = Developer.prisma().delete(
            where={
                'name': 'febcgjbfj',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.DeveloperWhereUniqueInput,
        include: Optional[types.DeveloperInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Developer record.

        Parameters
        ----------
        where
            Developer filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Developer model

        Returns
        -------
        prisma.models.Developer
            The found Developer record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        developer = Developer.prisma().find_unique(
            where={
                'name': 'bageiegghg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.DeveloperWhereUniqueInput,
        include: Optional[types.DeveloperInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Developer record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Developer filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Developer model

        Returns
        -------
        prisma.models.Developer
            The found Developer record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        developer = Developer.prisma().find_unique_or_raise(
            where={
                'name': 'faidicegb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeveloperWhereInput] = None,
        cursor: Optional[types.DeveloperWhereUniqueInput] = None,
        include: Optional[types.DeveloperInclude] = None,
        order: Optional[Union[types.DeveloperOrderByInput, List[types.DeveloperOrderByInput]]] = None,
        distinct: Optional[List[types.DeveloperScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Developer records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Developer records returned
        skip
            Ignore the first N results
        where
            Developer filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Developer model
        order
            Order the returned Developer records by any field
        distinct
            Filter Developer records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Developer]
            The list of all Developer records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Developer records
        developers = Developer.prisma().find_many(take=10)

        # find the first 5 Developer records ordered by the name field
        developers = Developer.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeveloperWhereInput] = None,
        cursor: Optional[types.DeveloperWhereUniqueInput] = None,
        include: Optional[types.DeveloperInclude] = None,
        order: Optional[Union[types.DeveloperOrderByInput, List[types.DeveloperOrderByInput]]] = None,
        distinct: Optional[List[types.DeveloperScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Developer record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Developer filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Developer model
        order
            Order the returned Developer records by any field
        distinct
            Filter Developer records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Developer
            The first Developer record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Developer record ordered by the amount_games_published field
        developer = Developer.prisma().find_first(
            skip=1,
            order={
                'amount_games_published': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeveloperWhereInput] = None,
        cursor: Optional[types.DeveloperWhereUniqueInput] = None,
        include: Optional[types.DeveloperInclude] = None,
        order: Optional[Union[types.DeveloperOrderByInput, List[types.DeveloperOrderByInput]]] = None,
        distinct: Optional[List[types.DeveloperScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Developer record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Developer filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Developer model
        order
            Order the returned Developer records by any field
        distinct
            Filter Developer records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Developer
            The first Developer record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Developer record ordered by the name field
        developer = Developer.prisma().find_first_or_raise(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.DeveloperUpdateInput,
        where: types.DeveloperWhereUniqueInput,
        include: Optional[types.DeveloperInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Developer record.

        Parameters
        ----------
        data
            Developer record data specifying what to update
        where
            Developer filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Developer model

        Returns
        -------
        prisma.models.Developer
            The updated Developer record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        developer = Developer.prisma().update(
            where={
                'name': 'bacecgfhbe',
            },
            data={
                # data to update the Developer record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.DeveloperWhereUniqueInput,
        data: types.DeveloperUpsertInput,
        include: Optional[types.DeveloperInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Developer filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Developer model

        Returns
        -------
        prisma.models.Developer
            The created or updated Developer record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        developer = Developer.prisma().upsert(
            where={
                'name': 'ihcahiead',
            },
            data={
                'create': {
                    'name': 'ihcahiead',
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.DeveloperUpdateManyMutationInput,
        where: types.DeveloperWhereInput,
    ) -> int:
        """Update multiple Developer records

        Parameters
        ----------
        data
            Developer data to update the selected Developer records to
        where
            Filter to select the Developer records to update

        Returns
        -------
        int
            The total number of Developer records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Developer records
        total = Developer.prisma().update_many(
            data={
                'amount_games_published': 1874748096
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeveloperWhereInput] = None,
        cursor: Optional[types.DeveloperWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Developer records present in the database

        Parameters
        ----------
        select
            Select the Developer fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Developer filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeveloperCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Developer.prisma().count()

        # results: prisma.types.DeveloperCountAggregateOutput
        results = Developer.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.DeveloperCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeveloperWhereInput] = None,
        cursor: Optional[types.DeveloperWhereUniqueInput] = None,
    ) -> types.DeveloperCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.DeveloperCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeveloperWhereInput] = None,
        cursor: Optional[types.DeveloperWhereUniqueInput] = None,
    ) -> Union[int, types.DeveloperCountAggregateOutput]:
        """Count the number of Developer records present in the database

        Parameters
        ----------
        select
            Select the Developer fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Developer filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeveloperCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Developer.prisma().count()

        # results: prisma.types.DeveloperCountAggregateOutput
        results = Developer.prisma().count(
            select={
                '_all': True,
                'amount_games_published': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DeveloperCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.DeveloperWhereInput] = None
    ) -> int:
        """Delete multiple Developer records.

        Parameters
        ----------
        where
            Optional Developer filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Developer records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Developer records
        total = Developer.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.DeveloperScalarFieldKeys'],
        *,
        where: Optional['types.DeveloperWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DeveloperAvgAggregateInput'] = None,
        sum: Optional['types.DeveloperSumAggregateInput'] = None,
        min: Optional['types.DeveloperMinAggregateInput'] = None,
        max: Optional['types.DeveloperMaxAggregateInput'] = None,
        having: Optional['types.DeveloperScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DeveloperCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DeveloperScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DeveloperScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DeveloperGroupByOutput']:
        """Group Developer records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Developer fields to group records by
        where
            Developer filter to select records
        take
            Limit the maximum number of Developer records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DeveloperGroupByOutput]
            A list of dictionaries representing the Developer record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Developer records by name values
        # and count how many records are in each group
        results = Developer.prisma().group_by(
            ['name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PublisherActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Publisher]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Publisher.prisma().query_raw(
            'SELECT * FROM Publisher WHERE name = ?',
            'jbgijghgb',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Publisher
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Publisher.prisma().query_first(
            'SELECT * FROM Publisher WHERE amount_games_published = ?',
            769267518,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.PublisherCreateInput,
        include: Optional[types.PublisherInclude] = None
    ) -> _PrismaModelT:
        """Create a new Publisher record.

        Parameters
        ----------
        data
            Publisher record data
        include
            Specifies which relations should be loaded on the returned Publisher model

        Returns
        -------
        prisma.models.Publisher
            The created Publisher record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Publisher record from just the required fields
        publisher = Publisher.prisma().create(
            data={
                # data to create a Publisher record
                'name': 'icadbcehj',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.PublisherCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Publisher records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Publisher record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Publisher.prisma().create_many(
            data=[
                {
                    # data to create a Publisher record
                    'name': 'jchciaee',
                },
                {
                    # data to create a Publisher record
                    'name': 'deeificjd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.PublisherWhereUniqueInput,
        include: Optional[types.PublisherInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Publisher record.

        Parameters
        ----------
        where
            Publisher filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Publisher model

        Returns
        -------
        prisma.models.Publisher
            The deleted Publisher record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        publisher = Publisher.prisma().delete(
            where={
                'name': 'bbcbhebbda',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.PublisherWhereUniqueInput,
        include: Optional[types.PublisherInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Publisher record.

        Parameters
        ----------
        where
            Publisher filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Publisher model

        Returns
        -------
        prisma.models.Publisher
            The found Publisher record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        publisher = Publisher.prisma().find_unique(
            where={
                'name': 'bejfijgcfb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.PublisherWhereUniqueInput,
        include: Optional[types.PublisherInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Publisher record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Publisher filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Publisher model

        Returns
        -------
        prisma.models.Publisher
            The found Publisher record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        publisher = Publisher.prisma().find_unique_or_raise(
            where={
                'name': 'caifcbgii',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PublisherWhereInput] = None,
        cursor: Optional[types.PublisherWhereUniqueInput] = None,
        include: Optional[types.PublisherInclude] = None,
        order: Optional[Union[types.PublisherOrderByInput, List[types.PublisherOrderByInput]]] = None,
        distinct: Optional[List[types.PublisherScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Publisher records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Publisher records returned
        skip
            Ignore the first N results
        where
            Publisher filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Publisher model
        order
            Order the returned Publisher records by any field
        distinct
            Filter Publisher records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Publisher]
            The list of all Publisher records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Publisher records
        publishers = Publisher.prisma().find_many(take=10)

        # find the first 5 Publisher records ordered by the name field
        publishers = Publisher.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PublisherWhereInput] = None,
        cursor: Optional[types.PublisherWhereUniqueInput] = None,
        include: Optional[types.PublisherInclude] = None,
        order: Optional[Union[types.PublisherOrderByInput, List[types.PublisherOrderByInput]]] = None,
        distinct: Optional[List[types.PublisherScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Publisher record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Publisher filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Publisher model
        order
            Order the returned Publisher records by any field
        distinct
            Filter Publisher records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Publisher
            The first Publisher record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Publisher record ordered by the amount_games_published field
        publisher = Publisher.prisma().find_first(
            skip=1,
            order={
                'amount_games_published': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PublisherWhereInput] = None,
        cursor: Optional[types.PublisherWhereUniqueInput] = None,
        include: Optional[types.PublisherInclude] = None,
        order: Optional[Union[types.PublisherOrderByInput, List[types.PublisherOrderByInput]]] = None,
        distinct: Optional[List[types.PublisherScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Publisher record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Publisher filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Publisher model
        order
            Order the returned Publisher records by any field
        distinct
            Filter Publisher records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Publisher
            The first Publisher record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Publisher record ordered by the name field
        publisher = Publisher.prisma().find_first_or_raise(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.PublisherUpdateInput,
        where: types.PublisherWhereUniqueInput,
        include: Optional[types.PublisherInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Publisher record.

        Parameters
        ----------
        data
            Publisher record data specifying what to update
        where
            Publisher filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Publisher model

        Returns
        -------
        prisma.models.Publisher
            The updated Publisher record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        publisher = Publisher.prisma().update(
            where={
                'name': 'igaibbfgj',
            },
            data={
                # data to update the Publisher record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.PublisherWhereUniqueInput,
        data: types.PublisherUpsertInput,
        include: Optional[types.PublisherInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Publisher filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Publisher model

        Returns
        -------
        prisma.models.Publisher
            The created or updated Publisher record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        publisher = Publisher.prisma().upsert(
            where={
                'name': 'bggajdcbbi',
            },
            data={
                'create': {
                    'name': 'bggajdcbbi',
                },
                'update': {
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.PublisherUpdateManyMutationInput,
        where: types.PublisherWhereInput,
    ) -> int:
        """Update multiple Publisher records

        Parameters
        ----------
        data
            Publisher data to update the selected Publisher records to
        where
            Filter to select the Publisher records to update

        Returns
        -------
        int
            The total number of Publisher records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Publisher records
        total = Publisher.prisma().update_many(
            data={
                'amount_games_published': 525761943
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PublisherWhereInput] = None,
        cursor: Optional[types.PublisherWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Publisher records present in the database

        Parameters
        ----------
        select
            Select the Publisher fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Publisher filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PublisherCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Publisher.prisma().count()

        # results: prisma.types.PublisherCountAggregateOutput
        results = Publisher.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.PublisherCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PublisherWhereInput] = None,
        cursor: Optional[types.PublisherWhereUniqueInput] = None,
    ) -> types.PublisherCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.PublisherCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PublisherWhereInput] = None,
        cursor: Optional[types.PublisherWhereUniqueInput] = None,
    ) -> Union[int, types.PublisherCountAggregateOutput]:
        """Count the number of Publisher records present in the database

        Parameters
        ----------
        select
            Select the Publisher fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Publisher filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PublisherCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Publisher.prisma().count()

        # results: prisma.types.PublisherCountAggregateOutput
        results = Publisher.prisma().count(
            select={
                '_all': True,
                'amount_games_published': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PublisherCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.PublisherWhereInput] = None
    ) -> int:
        """Delete multiple Publisher records.

        Parameters
        ----------
        where
            Optional Publisher filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Publisher records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Publisher records
        total = Publisher.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.PublisherScalarFieldKeys'],
        *,
        where: Optional['types.PublisherWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PublisherAvgAggregateInput'] = None,
        sum: Optional['types.PublisherSumAggregateInput'] = None,
        min: Optional['types.PublisherMinAggregateInput'] = None,
        max: Optional['types.PublisherMaxAggregateInput'] = None,
        having: Optional['types.PublisherScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PublisherCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PublisherScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PublisherScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PublisherGroupByOutput']:
        """Group Publisher records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Publisher fields to group records by
        where
            Publisher filter to select records
        take
            Limit the maximum number of Publisher records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PublisherGroupByOutput]
            A list of dictionaries representing the Publisher record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Publisher records by name values
        # and count how many records are in each group
        results = Publisher.prisma().group_by(
            ['name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class GameDeveloperActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.GameDeveloper]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = GameDeveloper.prisma().query_raw(
            'SELECT * FROM GameDeveloper WHERE gameId = ?',
            736209796,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.GameDeveloper
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = GameDeveloper.prisma().query_first(
            'SELECT * FROM GameDeveloper WHERE developerName = ?',
            'ejdjahicb',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.GameDeveloperCreateInput,
        include: Optional[types.GameDeveloperInclude] = None
    ) -> _PrismaModelT:
        """Create a new GameDeveloper record.

        Parameters
        ----------
        data
            GameDeveloper record data
        include
            Specifies which relations should be loaded on the returned GameDeveloper model

        Returns
        -------
        prisma.models.GameDeveloper
            The created GameDeveloper record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a GameDeveloper record from just the required fields
        gamedeveloper = GameDeveloper.prisma().create(
            data={
                # data to create a GameDeveloper record
                'gameId': 639686562,
                'developerName': 'gfeaahdeh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.GameDeveloperCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple GameDeveloper records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of GameDeveloper record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = GameDeveloper.prisma().create_many(
            data=[
                {
                    # data to create a GameDeveloper record
                    'gameId': 1905261552,
                    'developerName': 'hihegjif',
                },
                {
                    # data to create a GameDeveloper record
                    'gameId': 1398328302,
                    'developerName': 'ifgaaagff',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.GameDeveloperWhereUniqueInput,
        include: Optional[types.GameDeveloperInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single GameDeveloper record.

        Parameters
        ----------
        where
            GameDeveloper filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned GameDeveloper model

        Returns
        -------
        prisma.models.GameDeveloper
            The deleted GameDeveloper record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamedeveloper = GameDeveloper.prisma().delete(
            where={
                # GameDeveloper where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.GameDeveloperWhereUniqueInput,
        include: Optional[types.GameDeveloperInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique GameDeveloper record.

        Parameters
        ----------
        where
            GameDeveloper filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameDeveloper model

        Returns
        -------
        prisma.models.GameDeveloper
            The found GameDeveloper record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamedeveloper = GameDeveloper.prisma().find_unique(
            where={
                # GameDeveloper where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.GameDeveloperWhereUniqueInput,
        include: Optional[types.GameDeveloperInclude] = None
    ) -> _PrismaModelT:
        """Find a unique GameDeveloper record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            GameDeveloper filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameDeveloper model

        Returns
        -------
        prisma.models.GameDeveloper
            The found GameDeveloper record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamedeveloper = GameDeveloper.prisma().find_unique_or_raise(
            where={
                # GameDeveloper where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameDeveloperWhereInput] = None,
        cursor: Optional[types.GameDeveloperWhereUniqueInput] = None,
        include: Optional[types.GameDeveloperInclude] = None,
        order: Optional[Union[types.GameDeveloperOrderByInput, List[types.GameDeveloperOrderByInput]]] = None,
        distinct: Optional[List[types.GameDeveloperScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple GameDeveloper records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of GameDeveloper records returned
        skip
            Ignore the first N results
        where
            GameDeveloper filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameDeveloper model
        order
            Order the returned GameDeveloper records by any field
        distinct
            Filter GameDeveloper records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.GameDeveloper]
            The list of all GameDeveloper records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 GameDeveloper records
        gamedevelopers = GameDeveloper.prisma().find_many(take=10)

        # find the first 5 GameDeveloper records ordered by the gameId field
        gamedevelopers = GameDeveloper.prisma().find_many(
            take=5,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameDeveloperWhereInput] = None,
        cursor: Optional[types.GameDeveloperWhereUniqueInput] = None,
        include: Optional[types.GameDeveloperInclude] = None,
        order: Optional[Union[types.GameDeveloperOrderByInput, List[types.GameDeveloperOrderByInput]]] = None,
        distinct: Optional[List[types.GameDeveloperScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single GameDeveloper record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameDeveloper filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameDeveloper model
        order
            Order the returned GameDeveloper records by any field
        distinct
            Filter GameDeveloper records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameDeveloper
            The first GameDeveloper record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameDeveloper record ordered by the developerName field
        gamedeveloper = GameDeveloper.prisma().find_first(
            skip=1,
            order={
                'developerName': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameDeveloperWhereInput] = None,
        cursor: Optional[types.GameDeveloperWhereUniqueInput] = None,
        include: Optional[types.GameDeveloperInclude] = None,
        order: Optional[Union[types.GameDeveloperOrderByInput, List[types.GameDeveloperOrderByInput]]] = None,
        distinct: Optional[List[types.GameDeveloperScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single GameDeveloper record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameDeveloper filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameDeveloper model
        order
            Order the returned GameDeveloper records by any field
        distinct
            Filter GameDeveloper records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameDeveloper
            The first GameDeveloper record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameDeveloper record ordered by the gameId field
        gamedeveloper = GameDeveloper.prisma().find_first_or_raise(
            skip=1,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.GameDeveloperUpdateInput,
        where: types.GameDeveloperWhereUniqueInput,
        include: Optional[types.GameDeveloperInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single GameDeveloper record.

        Parameters
        ----------
        data
            GameDeveloper record data specifying what to update
        where
            GameDeveloper filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned GameDeveloper model

        Returns
        -------
        prisma.models.GameDeveloper
            The updated GameDeveloper record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        gamedeveloper = GameDeveloper.prisma().update(
            where={
                # GameDeveloper where unique filter

            },
            data={
                # data to update the GameDeveloper record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.GameDeveloperWhereUniqueInput,
        data: types.GameDeveloperUpsertInput,
        include: Optional[types.GameDeveloperInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            GameDeveloper filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned GameDeveloper model

        Returns
        -------
        prisma.models.GameDeveloper
            The created or updated GameDeveloper record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamedeveloper = GameDeveloper.prisma().upsert(
            where={
                # GameDeveloper where unique filter
            },
            data={
                'create': {
                    # GameDeveloper data to be set if the record does not exist
                },
                'update': {
                    # GameDeveloper data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.GameDeveloperUpdateManyMutationInput,
        where: types.GameDeveloperWhereInput,
    ) -> int:
        """Update multiple GameDeveloper records

        Parameters
        ----------
        data
            GameDeveloper data to update the selected GameDeveloper records to
        where
            Filter to select the GameDeveloper records to update

        Returns
        -------
        int
            The total number of GameDeveloper records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all GameDeveloper records
        total = GameDeveloper.prisma().update_many(
            data={
                'developerName': 'befcddgjce'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameDeveloperWhereInput] = None,
        cursor: Optional[types.GameDeveloperWhereUniqueInput] = None,
    ) -> int:
        """Count the number of GameDeveloper records present in the database

        Parameters
        ----------
        select
            Select the GameDeveloper fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameDeveloper filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameDeveloperCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GameDeveloper.prisma().count()

        # results: prisma.types.GameDeveloperCountAggregateOutput
        results = GameDeveloper.prisma().count(
            select={
                '_all': True,
                'gameId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.GameDeveloperCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameDeveloperWhereInput] = None,
        cursor: Optional[types.GameDeveloperWhereUniqueInput] = None,
    ) -> types.GameDeveloperCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.GameDeveloperCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameDeveloperWhereInput] = None,
        cursor: Optional[types.GameDeveloperWhereUniqueInput] = None,
    ) -> Union[int, types.GameDeveloperCountAggregateOutput]:
        """Count the number of GameDeveloper records present in the database

        Parameters
        ----------
        select
            Select the GameDeveloper fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameDeveloper filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameDeveloperCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GameDeveloper.prisma().count()

        # results: prisma.types.GameDeveloperCountAggregateOutput
        results = GameDeveloper.prisma().count(
            select={
                '_all': True,
                'developerName': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.GameDeveloperCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.GameDeveloperWhereInput] = None
    ) -> int:
        """Delete multiple GameDeveloper records.

        Parameters
        ----------
        where
            Optional GameDeveloper filter to find the records to be deleted

        Returns
        -------
        int
            The total number of GameDeveloper records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all GameDeveloper records
        total = GameDeveloper.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.GameDeveloperScalarFieldKeys'],
        *,
        where: Optional['types.GameDeveloperWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.GameDeveloperAvgAggregateInput'] = None,
        sum: Optional['types.GameDeveloperSumAggregateInput'] = None,
        min: Optional['types.GameDeveloperMinAggregateInput'] = None,
        max: Optional['types.GameDeveloperMaxAggregateInput'] = None,
        having: Optional['types.GameDeveloperScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.GameDeveloperCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.GameDeveloperScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.GameDeveloperScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.GameDeveloperGroupByOutput']:
        """Group GameDeveloper records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar GameDeveloper fields to group records by
        where
            GameDeveloper filter to select records
        take
            Limit the maximum number of GameDeveloper records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.GameDeveloperGroupByOutput]
            A list of dictionaries representing the GameDeveloper record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group GameDeveloper records by gameId values
        # and count how many records are in each group
        results = GameDeveloper.prisma().group_by(
            ['gameId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class GamePublisherActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.GamePublisher]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = GamePublisher.prisma().query_raw(
            'SELECT * FROM GamePublisher WHERE gameId = ?',
            1573199653,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.GamePublisher
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = GamePublisher.prisma().query_first(
            'SELECT * FROM GamePublisher WHERE publisherName = ?',
            'cabdjadaji',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.GamePublisherCreateInput,
        include: Optional[types.GamePublisherInclude] = None
    ) -> _PrismaModelT:
        """Create a new GamePublisher record.

        Parameters
        ----------
        data
            GamePublisher record data
        include
            Specifies which relations should be loaded on the returned GamePublisher model

        Returns
        -------
        prisma.models.GamePublisher
            The created GamePublisher record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a GamePublisher record from just the required fields
        gamepublisher = GamePublisher.prisma().create(
            data={
                # data to create a GamePublisher record
                'gameId': 500965035,
                'publisherName': 'biaagcedjc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.GamePublisherCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple GamePublisher records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of GamePublisher record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = GamePublisher.prisma().create_many(
            data=[
                {
                    # data to create a GamePublisher record
                    'gameId': 2077067425,
                    'publisherName': 'bghcbbcidi',
                },
                {
                    # data to create a GamePublisher record
                    'gameId': 926677639,
                    'publisherName': 'beehgcebbg',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.GamePublisherWhereUniqueInput,
        include: Optional[types.GamePublisherInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single GamePublisher record.

        Parameters
        ----------
        where
            GamePublisher filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned GamePublisher model

        Returns
        -------
        prisma.models.GamePublisher
            The deleted GamePublisher record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamepublisher = GamePublisher.prisma().delete(
            where={
                # GamePublisher where unique filter

            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.GamePublisherWhereUniqueInput,
        include: Optional[types.GamePublisherInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique GamePublisher record.

        Parameters
        ----------
        where
            GamePublisher filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GamePublisher model

        Returns
        -------
        prisma.models.GamePublisher
            The found GamePublisher record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamepublisher = GamePublisher.prisma().find_unique(
            where={
                # GamePublisher where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.GamePublisherWhereUniqueInput,
        include: Optional[types.GamePublisherInclude] = None
    ) -> _PrismaModelT:
        """Find a unique GamePublisher record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            GamePublisher filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GamePublisher model

        Returns
        -------
        prisma.models.GamePublisher
            The found GamePublisher record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamepublisher = GamePublisher.prisma().find_unique_or_raise(
            where={
                # GamePublisher where unique filter

            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GamePublisherWhereInput] = None,
        cursor: Optional[types.GamePublisherWhereUniqueInput] = None,
        include: Optional[types.GamePublisherInclude] = None,
        order: Optional[Union[types.GamePublisherOrderByInput, List[types.GamePublisherOrderByInput]]] = None,
        distinct: Optional[List[types.GamePublisherScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple GamePublisher records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of GamePublisher records returned
        skip
            Ignore the first N results
        where
            GamePublisher filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GamePublisher model
        order
            Order the returned GamePublisher records by any field
        distinct
            Filter GamePublisher records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.GamePublisher]
            The list of all GamePublisher records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 GamePublisher records
        gamepublishers = GamePublisher.prisma().find_many(take=10)

        # find the first 5 GamePublisher records ordered by the gameId field
        gamepublishers = GamePublisher.prisma().find_many(
            take=5,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GamePublisherWhereInput] = None,
        cursor: Optional[types.GamePublisherWhereUniqueInput] = None,
        include: Optional[types.GamePublisherInclude] = None,
        order: Optional[Union[types.GamePublisherOrderByInput, List[types.GamePublisherOrderByInput]]] = None,
        distinct: Optional[List[types.GamePublisherScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single GamePublisher record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GamePublisher filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GamePublisher model
        order
            Order the returned GamePublisher records by any field
        distinct
            Filter GamePublisher records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GamePublisher
            The first GamePublisher record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GamePublisher record ordered by the publisherName field
        gamepublisher = GamePublisher.prisma().find_first(
            skip=1,
            order={
                'publisherName': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GamePublisherWhereInput] = None,
        cursor: Optional[types.GamePublisherWhereUniqueInput] = None,
        include: Optional[types.GamePublisherInclude] = None,
        order: Optional[Union[types.GamePublisherOrderByInput, List[types.GamePublisherOrderByInput]]] = None,
        distinct: Optional[List[types.GamePublisherScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single GamePublisher record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GamePublisher filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GamePublisher model
        order
            Order the returned GamePublisher records by any field
        distinct
            Filter GamePublisher records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GamePublisher
            The first GamePublisher record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GamePublisher record ordered by the gameId field
        gamepublisher = GamePublisher.prisma().find_first_or_raise(
            skip=1,
            order={
                'gameId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.GamePublisherUpdateInput,
        where: types.GamePublisherWhereUniqueInput,
        include: Optional[types.GamePublisherInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single GamePublisher record.

        Parameters
        ----------
        data
            GamePublisher record data specifying what to update
        where
            GamePublisher filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned GamePublisher model

        Returns
        -------
        prisma.models.GamePublisher
            The updated GamePublisher record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        gamepublisher = GamePublisher.prisma().update(
            where={
                # GamePublisher where unique filter

            },
            data={
                # data to update the GamePublisher record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.GamePublisherWhereUniqueInput,
        data: types.GamePublisherUpsertInput,
        include: Optional[types.GamePublisherInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            GamePublisher filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned GamePublisher model

        Returns
        -------
        prisma.models.GamePublisher
            The created or updated GamePublisher record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamepublisher = GamePublisher.prisma().upsert(
            where={
                # GamePublisher where unique filter
            },
            data={
                'create': {
                    # GamePublisher data to be set if the record does not exist
                },
                'update': {
                    # GamePublisher data to be set if the record does exist
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.GamePublisherUpdateManyMutationInput,
        where: types.GamePublisherWhereInput,
    ) -> int:
        """Update multiple GamePublisher records

        Parameters
        ----------
        data
            GamePublisher data to update the selected GamePublisher records to
        where
            Filter to select the GamePublisher records to update

        Returns
        -------
        int
            The total number of GamePublisher records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all GamePublisher records
        total = GamePublisher.prisma().update_many(
            data={
                'publisherName': 'bhdiaidiaf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GamePublisherWhereInput] = None,
        cursor: Optional[types.GamePublisherWhereUniqueInput] = None,
    ) -> int:
        """Count the number of GamePublisher records present in the database

        Parameters
        ----------
        select
            Select the GamePublisher fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GamePublisher filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GamePublisherCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GamePublisher.prisma().count()

        # results: prisma.types.GamePublisherCountAggregateOutput
        results = GamePublisher.prisma().count(
            select={
                '_all': True,
                'gameId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.GamePublisherCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GamePublisherWhereInput] = None,
        cursor: Optional[types.GamePublisherWhereUniqueInput] = None,
    ) -> types.GamePublisherCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.GamePublisherCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GamePublisherWhereInput] = None,
        cursor: Optional[types.GamePublisherWhereUniqueInput] = None,
    ) -> Union[int, types.GamePublisherCountAggregateOutput]:
        """Count the number of GamePublisher records present in the database

        Parameters
        ----------
        select
            Select the GamePublisher fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GamePublisher filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GamePublisherCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = GamePublisher.prisma().count()

        # results: prisma.types.GamePublisherCountAggregateOutput
        results = GamePublisher.prisma().count(
            select={
                '_all': True,
                'publisherName': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.GamePublisherCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.GamePublisherWhereInput] = None
    ) -> int:
        """Delete multiple GamePublisher records.

        Parameters
        ----------
        where
            Optional GamePublisher filter to find the records to be deleted

        Returns
        -------
        int
            The total number of GamePublisher records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all GamePublisher records
        total = GamePublisher.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.GamePublisherScalarFieldKeys'],
        *,
        where: Optional['types.GamePublisherWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.GamePublisherAvgAggregateInput'] = None,
        sum: Optional['types.GamePublisherSumAggregateInput'] = None,
        min: Optional['types.GamePublisherMinAggregateInput'] = None,
        max: Optional['types.GamePublisherMaxAggregateInput'] = None,
        having: Optional['types.GamePublisherScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.GamePublisherCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.GamePublisherScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.GamePublisherScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.GamePublisherGroupByOutput']:
        """Group GamePublisher records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar GamePublisher fields to group records by
        where
            GamePublisher filter to select records
        take
            Limit the maximum number of GamePublisher records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.GamePublisherGroupByOutput]
            A list of dictionaries representing the GamePublisher record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group GamePublisher records by gameId values
        # and count how many records are in each group
        results = GamePublisher.prisma().group_by(
            ['gameId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReviewActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Review]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Review.prisma().query_raw(
            'SELECT * FROM Review WHERE id = ?',
            'deajegcfi',
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Review
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Review.prisma().query_first(
            'SELECT * FROM Review WHERE gameId = ?',
            601077795,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ReviewCreateInput,
        include: Optional[types.ReviewInclude] = None
    ) -> _PrismaModelT:
        """Create a new Review record.

        Parameters
        ----------
        data
            Review record data
        include
            Specifies which relations should be loaded on the returned Review model

        Returns
        -------
        prisma.models.Review
            The created Review record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Review record from just the required fields
        review = Review.prisma().create(
            data={
                # data to create a Review record
                'id': 'cjagadcjg',
                'gameId': 1855826649,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ReviewCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Review records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Review record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Review.prisma().create_many(
            data=[
                {
                    # data to create a Review record
                    'id': 'bgbbaajbic',
                    'gameId': 446673791,
                },
                {
                    # data to create a Review record
                    'id': 'daafgidjg',
                    'gameId': 632626069,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ReviewWhereUniqueInput,
        include: Optional[types.ReviewInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Review record.

        Parameters
        ----------
        where
            Review filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Review model

        Returns
        -------
        prisma.models.Review
            The deleted Review record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        review = Review.prisma().delete(
            where={
                'id': 'bhceabbgja',
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ReviewWhereUniqueInput,
        include: Optional[types.ReviewInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Review record.

        Parameters
        ----------
        where
            Review filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Review model

        Returns
        -------
        prisma.models.Review
            The found Review record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        review = Review.prisma().find_unique(
            where={
                'id': 'ehabfhegh',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ReviewWhereUniqueInput,
        include: Optional[types.ReviewInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Review record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Review filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Review model

        Returns
        -------
        prisma.models.Review
            The found Review record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        review = Review.prisma().find_unique_or_raise(
            where={
                'id': 'bcajcajjbc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReviewWhereInput] = None,
        cursor: Optional[types.ReviewWhereUniqueInput] = None,
        include: Optional[types.ReviewInclude] = None,
        order: Optional[Union[types.ReviewOrderByInput, List[types.ReviewOrderByInput]]] = None,
        distinct: Optional[List[types.ReviewScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Review records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Review records returned
        skip
            Ignore the first N results
        where
            Review filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Review model
        order
            Order the returned Review records by any field
        distinct
            Filter Review records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Review]
            The list of all Review records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Review records
        reviews = Review.prisma().find_many(take=10)

        # find the first 5 Review records ordered by the authorSteamId field
        reviews = Review.prisma().find_many(
            take=5,
            order={
                'authorSteamId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReviewWhereInput] = None,
        cursor: Optional[types.ReviewWhereUniqueInput] = None,
        include: Optional[types.ReviewInclude] = None,
        order: Optional[Union[types.ReviewOrderByInput, List[types.ReviewOrderByInput]]] = None,
        distinct: Optional[List[types.ReviewScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Review record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Review filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Review model
        order
            Order the returned Review records by any field
        distinct
            Filter Review records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Review
            The first Review record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Review record ordered by the authorPlaytimeForever field
        review = Review.prisma().find_first(
            skip=1,
            order={
                'authorPlaytimeForever': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReviewWhereInput] = None,
        cursor: Optional[types.ReviewWhereUniqueInput] = None,
        include: Optional[types.ReviewInclude] = None,
        order: Optional[Union[types.ReviewOrderByInput, List[types.ReviewOrderByInput]]] = None,
        distinct: Optional[List[types.ReviewScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Review record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Review filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Review model
        order
            Order the returned Review records by any field
        distinct
            Filter Review records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Review
            The first Review record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Review record ordered by the authorPlaytimeAtReview field
        review = Review.prisma().find_first_or_raise(
            skip=1,
            order={
                'authorPlaytimeAtReview': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ReviewUpdateInput,
        where: types.ReviewWhereUniqueInput,
        include: Optional[types.ReviewInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Review record.

        Parameters
        ----------
        data
            Review record data specifying what to update
        where
            Review filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Review model

        Returns
        -------
        prisma.models.Review
            The updated Review record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        review = Review.prisma().update(
            where={
                'id': 'bfdgheeegf',
            },
            data={
                # data to update the Review record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ReviewWhereUniqueInput,
        data: types.ReviewUpsertInput,
        include: Optional[types.ReviewInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Review filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Review model

        Returns
        -------
        prisma.models.Review
            The created or updated Review record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        review = Review.prisma().upsert(
            where={
                'id': 'ececbijji',
            },
            data={
                'create': {
                    'id': 'ececbijji',
                    'gameId': 632626069,
                },
                'update': {
                    'gameId': 632626069,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ReviewUpdateManyMutationInput,
        where: types.ReviewWhereInput,
    ) -> int:
        """Update multiple Review records

        Parameters
        ----------
        data
            Review data to update the selected Review records to
        where
            Filter to select the Review records to update

        Returns
        -------
        int
            The total number of Review records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Review records
        total = Review.prisma().update_many(
            data={
                'authorLastPlayed': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReviewWhereInput] = None,
        cursor: Optional[types.ReviewWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Review records present in the database

        Parameters
        ----------
        select
            Select the Review fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Review filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReviewCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Review.prisma().count()

        # results: prisma.types.ReviewCountAggregateOutput
        results = Review.prisma().count(
            select={
                '_all': True,
                'language': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ReviewCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReviewWhereInput] = None,
        cursor: Optional[types.ReviewWhereUniqueInput] = None,
    ) -> types.ReviewCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ReviewCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReviewWhereInput] = None,
        cursor: Optional[types.ReviewWhereUniqueInput] = None,
    ) -> Union[int, types.ReviewCountAggregateOutput]:
        """Count the number of Review records present in the database

        Parameters
        ----------
        select
            Select the Review fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Review filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReviewCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Review.prisma().count()

        # results: prisma.types.ReviewCountAggregateOutput
        results = Review.prisma().count(
            select={
                '_all': True,
                'review': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReviewCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ReviewWhereInput] = None
    ) -> int:
        """Delete multiple Review records.

        Parameters
        ----------
        where
            Optional Review filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Review records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Review records
        total = Review.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ReviewScalarFieldKeys'],
        *,
        where: Optional['types.ReviewWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReviewAvgAggregateInput'] = None,
        sum: Optional['types.ReviewSumAggregateInput'] = None,
        min: Optional['types.ReviewMinAggregateInput'] = None,
        max: Optional['types.ReviewMaxAggregateInput'] = None,
        having: Optional['types.ReviewScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReviewCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReviewScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReviewScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReviewGroupByOutput']:
        """Group Review records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Review fields to group records by
        where
            Review filter to select records
        take
            Limit the maximum number of Review records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReviewGroupByOutput]
            A list of dictionaries representing the Review record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Review records by votedUp values
        # and count how many records are in each group
        results = Review.prisma().group_by(
            ['votedUp'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models